{
    "name": "sample-node-app",
    "version": "1.0.0",
    "description": "A sample Node.js app for Jenkins CI/CD pipeline.",
    "main": "app.js",
    "scripts": {
      "test": "mocha test/**/*.test.js",
      "build": "echo 'Building the application...' && mkdir -p build && echo 'App built!' > build/output.txt"
    },
    "dependencies": {
      "express": "^4.17.1"
    },
    "devDependencies": {
      "mocha": "^9.1.3"
    },
    "author": "Your Name",
    "license": "ISC"
  }
  